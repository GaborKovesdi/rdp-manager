<Window x:Class="RdpManager.Views.MasterPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="RDP Manager" Height="300" Width="440"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Icon="pack://application:,,,/app.ico">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadingText" Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock x:Name="DescriptionText" Grid.Row="1" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Mesterjelszó" Margin="0,0,0,4"/>
            <PasswordBox x:Name="MasterBox" Padding="4" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel x:Name="ConfirmPanel" Grid.Row="3">
            <TextBlock Text="Mesterjelszó még egyszer" Margin="0,0,0,4"/>
            <PasswordBox x:Name="ConfirmBox" Padding="4" Margin="0,0,0,10"/>
        </StackPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="4" Foreground="Red" TextWrapping="Wrap" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Kilépés" Width="90" Padding="0,6" Margin="0,0,8,0" Click="CancelButton_Click" IsCancel="True"/>
            <Button x:Name="OkButton" Width="110" Padding="0,6" Click="OkButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
